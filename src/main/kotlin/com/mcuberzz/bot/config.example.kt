// package com.mcuberzz.bot
// TODO: Rename this file to `config.kt` and uncomment the line above

import dev.kord.common.Color
import dev.kord.common.entity.Snowflake

// Bot 🤖
const val botActivity = "with a rubik's brand"
const val botToken = ""

// Log Channel 🪵
val eventChannel = Snowflake("844740227443720192")

// Colours 🌈
val botPrimaryColour = Color(0,116,255 )
val botSuccessColour = Color(0,255,127)
val botErrorColour = Color(255,0,0)

// Cool People 😎
val admins = arrayOf(Snowflake("000000000000000000"))
